# 🔒 Security Policy

## Обзор безопасности

Это приложение реализует несколько уровней защиты для обеспечения безопасности пользовательских данных.

## Реализованные меры безопасности

### 1. Аутентификация и авторизация

✅ **OTP Authentication**
- Вход через одноразовые коды вместо паролей
- Коды отправляются на email и действительны ограниченное время
- Защита от брутфорса на уровне Supabase

✅ **Email Whitelist**
- Опциональное ограничение доступа по email
- Проверка на клиенте и через RLS на сервере

✅ **Row Level Security (RLS)**
- Все таблицы защищены RLS политиками
- Пользователи видят только свои данные
- Проверка `auth.uid()` на каждом запросе

### 2. Хранение токенов

✅ **Cookie Storage с fallback**
- Приоритет: secure cookies с `sameSite: lax`
- Fallback на localStorage для iOS совместимости
- Автоматическое обновление токенов

✅ **PKCE Flow**
- Использование PKCE вместо implicit flow
- Защита от перехвата токенов через URL
- Защита от CSRF атак

⚠️ **Ограничение: HttpOnly cookies**
- Cookies доступны из JavaScript (требуется для Supabase SDK)
- Компенсируется через CSP и другие меры защиты

### 3. Защита от XSS

✅ **Content Security Policy (CSP)**
- Строгие политики для скриптов и стилей
- Ограничение источников загрузки ресурсов
- Запрет на inline scripts (где возможно)

✅ **React Auto-escaping**
- Весь пользовательский контент экранируется автоматически
- Не используется `innerHTML`, `dangerouslySetInnerHTML`, `eval()`

✅ **Security Headers**
```
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Referrer-Policy: strict-origin-when-cross-origin
Permissions-Policy: geolocation=(), microphone=(), camera=()
```

### 4. Защита от SQL Injection

✅ **Supabase SDK**
- Автоматическая параметризация запросов
- Нет прямых SQL-запросов в клиентском коде

### 5. Защита данных

✅ **HTTPS Only**
- Принудительное использование HTTPS в production
- HSTS header для предотвращения downgrade атак

✅ **Environment Variables**
- Все секреты в `.env` файлах
- `.env.local` исключен из git
- Только публичные ключи (anon key) на клиенте

## Checklist для deployment

Перед деплоем в production:

### Обязательно:

- [ ] Ротация ключей если `.env.local` попал в git
- [ ] Проверка RLS политик в Supabase Dashboard
- [ ] Настройка Email whitelist (если требуется)
- [ ] Включение HTTPS
- [ ] Настройка CORS в Supabase (только ваш домен)
- [ ] Проверка Security Headers (используйте https://securityheaders.com)

### Рекомендуется:

- [ ] Настройка rate limiting в Supabase
- [ ] Мониторинг аномальных запросов
- [ ] Регулярные бэкапы БД
- [ ] Настройка email уведомлений о подозрительной активности

## Проверка безопасности ключей

### Проверить попадание .env.local в git:

```bash
# Проверка текущего состояния
git status

# Проверка истории
git log --all --full-history --source -- ".env.local"

# Проверка содержимого всех коммитов
git grep "SUPABASE_ANON_KEY" $(git rev-list --all)
```

### Если ключи скомпрометированы:

1. **Ротация ключей в Supabase:**
   - Зайдите в Supabase Dashboard → Settings → API
   - Сгенерируйте новый anon key
   - Обновите `.env.local` и переменные в Vercel/Docker

2. **Очистка git history (ОПАСНО!):**
   ```bash
   # Вариант 1: BFG Repo-Cleaner (рекомендуется)
   brew install bfg
   bfg --delete-files .env.local
   git reflog expire --expire=now --all
   git gc --prune=now --aggressive

   # Вариант 2: git filter-branch
   git filter-branch --force --index-filter \
     'git rm --cached --ignore-unmatch .env.local' \
     --prune-empty --tag-name-filter cat -- --all
   ```

3. **Force push (координируйте с командой!):**
   ```bash
   git push --force --all
   git push --force --tags
   ```

## Отчет о уязвимостях

Если вы обнаружили уязвимость:

1. **НЕ создавайте публичный issue**
2. Отправьте email на: danya.brain@gmail.com
3. Опишите уязвимость и шаги для воспроизведения
4. Мы ответим в течение 48 часов

## Известные ограничения

### 1. Cookies не HttpOnly
**Причина:** Supabase SDK требует доступ из JavaScript
**Риск:** Средний - XSS может украсть токены
**Митигация:** CSP, автоматическое экранирование React

### 2. Inline scripts в CSP
**Причина:** Vite и некоторые библиотеки используют inline scripts
**Риск:** Средний - сложнее защита от XSS
**Митигация:** Строгие политики, nonce в будущем

### 3. Email whitelist на клиенте
**Причина:** Быстрая проверка до отправки OTP
**Риск:** Низкий - основная защита через RLS
**Митигация:** Дублирование проверки на сервере через RLS

## Регулярные проверки

Выполняйте регулярно:

```bash
# Проверка зависимостей на уязвимости
npm audit
npm audit fix

# Обновление зависимостей
npm update

# Проверка security headers
curl -I https://your-domain.com | grep -E "(X-Frame|Content-Security|Strict-Transport)"
```

## Дополнительные ресурсы

- [Supabase Security Best Practices](https://supabase.com/docs/guides/auth/row-level-security)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Mozilla Web Security Guidelines](https://infosec.mozilla.org/guidelines/web_security)
- [Content Security Policy Reference](https://content-security-policy.com/)

---

**Последнее обновление:** 2026-01-19
**Версия:** 1.0.0
